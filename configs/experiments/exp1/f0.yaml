model_type: "single"
experiment: "exp1_f0"
embeddings:
  - {name: "f0", dim: 1}
adapter:
  adapter_dim: 256
mlp:
  hidden_dim: 512
  num_layers: 2
  dropout: 0.1
  activation: "relu"
sigmoid_scale: true
